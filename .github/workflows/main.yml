# This workflow has been split into separate files for independent troubleshooting.
# See ci.yml (orchestrator), build.yml, test-unit.yml, test-integration.yml,
# test-e2e.yml, and package-publish.yml.
#
# The original monolithic workflow is preserved in main.yml.bak.
name: "Delta-V CI (deprecated - see ci.yml)"
on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "This workflow is deprecated. Use ci.yml instead."
        required: true
        default: "I understand"
jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "This workflow has been split. Please use ci.yml instead."
          echo "See: .github/workflows/ci.yml"
          exit 1
